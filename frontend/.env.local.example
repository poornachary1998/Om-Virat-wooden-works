# URL of your Strapi server. No trailing slash.
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337

# Read-only API token from Strapi (Settings > API Tokens).
# Only needed if you set your content types to private.
STRAPI_API_TOKEN=
